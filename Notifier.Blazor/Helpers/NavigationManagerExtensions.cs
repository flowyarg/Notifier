using Microsoft.AspNetCore.Components;
using Notifier.Blazor.Controllers;
using Notifier.Blazor.Helpers.Routes;

namespace Notifier.Blazor.Helpers
{
    //internal static class NavigationManagerExtensions
    //{
    //    public static void NavigateToStartAuthentication(this NavigationManager navigationManager)
    //    {
    //        var url = ApiControllerRoutesHelper.GetActionUrl<VkTokenController>(c => c.StartImplicitAuthentication);

    //        navigationManager.NavigateTo(url);
    //        //Idk what the hell - it works only with additional refresh?
    //        navigationManager.Refresh();
    //    }

    //    public static void NavigateToEndAuthentication(this NavigationManager navigationManager, string code)
    //    {
    //        var url = ApiControllerRoutesHelper.GetActionUrl<VkTokenController>(c => c.EndImplicitAuthentication);
    //        url += $"?code={code}";
    //        navigationManager.NavigateTo(url);
    //        //Idk what the hell - it works only with additional refresh?
    //        navigationManager.Refresh();
    //    }
    //}
}
